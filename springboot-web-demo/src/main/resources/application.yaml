spring:
  mvc:
    hiddenmethod:
      filter:
        enabled: true
    contentnegotiation:
      favor-parameter: true
#    static-path-pattern: /res/**
server:
  servlet:
    context-path: /atguigu
